// Entry point for the JSON-vs-TOON benchmark console application.
namespace GlyphToon.Benchmark;

internal static class Program
{
    private static int Main(string[] args) => BenchmarkConsoleApp.Run(args);
}
